// Display-only mirror of the server's authoritative ToS version
// (netlify/functions/_shared/tos.ts). Keep these two values in sync. The server
// is the source of truth for what gets stored on the user doc.
export const TOS_VERSION = '1.0'
